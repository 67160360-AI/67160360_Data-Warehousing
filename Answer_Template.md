# รายงานแลป OLTP OLAP และ Pivot

**ชื่อ:** ภัครพล ริตกันโต  
**รหัสประจำตัว:** 67160360  
**กลุ่มเรียน:** 2  

---

## 1 OLTP

### โค้ดที่เติมใน `oltp_demo.py`
```python
# Guarded UPDATE using WHERE order_id and status
cur = con.execute("UPDATE orders SET status = 'PAID' WHERE order_id = 'O1004' AND status = 'PENDING'")
print(f'Rows updated (rowcount): {cur.rowcount}')
```

### ผลการรันสองรอบ
* **รอบที่ 1:**
  ```text
  Before: [('O1004', 'PENDING')]
  Rows updated (rowcount): 1
  After: [('O1004', 'PAID')]
  ```
* **รอบที่ 2:**
  ```text
  Before: [('O1004', 'PAID')]
  Rows updated (rowcount): 0
  After: [('O1004', 'PAID')]
  ```

### คำอธิบาย
1. **จำนวนแถวที่แก้ไข:** รอบแรกแก้ไข 1 แถว | รอบสองแก้ไข 0 แถว
2. **เหตุผลที่ต้องมีเงื่อนไข `status` (Guarded Update):**
   * **Valid State Transition:** ตรวจสอบความถูกต้องของลำดับการเปลี่ยนสถานะ โดยอนุญาตให้อัปเดตเป็น `PAID` ได้เฉพาะออเดอร์ที่มีสถานะ `PENDING` เท่านั้น
   * **Idempotency:** ทำให้การรันซ้ำไม่เกิด Side effect ข้อมูลไม่ถูกแก้ไขซ้ำซ้อน
   * **Concurrency Control:** ป้องกัน Lost Update ในระบบที่มีผู้ใช้งานหลายคนพร้อมกัน หากมีธุรกรรมอื่นเปลี่ยนสถานะไปก่อน (เช่น `CANCELLED`) ระบบจะระงับการอัปเดตทันที
3. **เหตุใดเป็น OLTP แม้มีคำสั่ง SELECT:** เพราะเป็นการประมวลผลธุรกรรมระดับปฏิบัติการประจำวัน (Operational) ระดับแถวเดี่ยว (Row-level) ตามหลัก ACID คำสั่ง SELECT เป็นเพียงการค้นหาสถานะเฉพาะเจาะจงผ่าน Primary Key (Point Lookup) ไม่ใช่การประมวลผลเชิงวิเคราะห์ข้อมูลขนาดใหญ่ (OLAP)
4. **ความสัมพันธ์ระหว่าง `oltp.db` และ `warehouse.db`:**
   * การเปลี่ยน `oltp.db` **ไม่ทำให้ยอดใน `warehouse.db` เปลี่ยนทันที** เพราะระบบแยกฐานข้อมูลออกจากกันเพื่อไม่ให้โหลดงานกระทบกัน
   * ในระบบจริงต้องเชื่อมโยงด้วยกระบวนการ **ETL (Extract, Transform, Load)** หรือ **CDC (Change Data Capture)** นำข้อมูลเข้าสู่ Data Warehouse ตามรอบเวลา (Batch) หรือ Near Real-time Stream

---

## 2 Grain และ Star Schema

### แผนภาพ Star Schema
```text
      +------------------+
      |     dim_date     |
      +------------------+
      | PK date_key      |
      |    full_date     |
      |    year          |
      |    month         |
      +--------+---------+
               | 1
               |
               | N
      +--------+---------+          +------------------+
      |    fact_sales    | N      1 |   dim_product    |
      +------------------+----------+------------------+
      | PK order_id      |          | PK product_key   |
      | PK line_no       |          |    product_name  |
      | FK date_key      |          |    category      |
      | FK product_key   |          +------------------+
      | FK store_key     |
      |    quantity      |
      |    unit_price    |
      +--------+---------+
               | N
               |
               | 1
      +--------+---------+
      |    dim_store     |
      +------------------+
      | PK store_key     |
      |    store_name    |
      |    province      |
      |    region        |
      +------------------+
```

### Grain และความสัมพันธ์
* **Grain:** "การขายสินค้า 1 รายการใน 1 ใบสั่งซื้อ (Line Item of an Order) ณ วันที่และสาขาที่ระบุ"
* **ตัวอย่าง O1001:** เป็นใบสั่งซื้อ 1 ใบ แต่บันทึก 2 แถวใน `fact_sales` เพราะลูกค้าซื้อสินค้า 2 รายการ (Tea 2 ชิ้น และ Cookie 1 ชิ้น)
* **PK / FK:** `fact_sales` มี Composite PK คือ `(order_id, line_no)` และมี FK คือ `date_key`, `product_key`, `store_key` เชื่อมโยง 1:N กับตารางมิติ
* **3 Dimensions & 2 Measures:**
  * Dimensions: เวลา (`dim_date`), สินค้า (`dim_product`), สถานที่/สาขา (`dim_store`)
  * Measures: `quantity` (ชิ้น), `amount` (บาท)
* **Hierarchy:**
  * เวลา: `full_date` (วัน) $\rightarrow$ `month` (เดือน) $\rightarrow$ `year` (ปี)
  * สถานที่: `store_name` (สาขา) $\rightarrow$ `province` (จังหวัด) $\rightarrow$ `region` (ภูมิภาค)
* **ทำไมไม่ SUM `unit_price`:** เพราะ `unit_price` เป็นราคาต่อหน่วย (Non-additive measure) ไม่ได้ถ่วงน้ำหนักตามจำนวนชิ้น การบวกราคาป้ายไม่มีความหมายทางธุรกิจ ตัวเลขที่สะท้อนยอดขายจริงคือ `amount` ($quantity \times unit\_price$)

### ผลการรัน `q01.sql`
```text
line_count	order_count	units	revenue
8	6	23	1390
```
* **จำนวนรายการ:** 8 รายการ
* **จำนวนออเดอร์:** 6 ออเดอร์
* **จำนวนชิ้น:** 23 ชิ้น
* **ยอดขายรวม:** 1,390 บาท

---

## 3 OLAP

### สรุปผลลัพธ์คิวรี (q02–q07, q12)
| ไฟล์ | Operation / รายละเอียด | ผลลัพธ์ที่ได้ |
| :--- | :--- | :--- |
| **q02.sql** | Roll-up ยอดขายรายเดือน | `2026-08`: 490 บาท, `2026-09`: 900 บาท (รวม 1,390 บาท) |
| **q03.sql** | เพิ่มมิติสถานที่ (จังหวัด) | ส.ค.: Bangkok 310, Chonburi 180<br>ก.ย.: Bangkok 540, Chonburi 360 |
| **q04.sql** | Drill-down มิติเวลาลงระดับวัน (ก.ย.) | `2026-09-09`: 360, `2026-09-10`: 300, `2026-09-11`: 240 บาท (รวม 900 บาท) |
| **q05.sql** | Slice เจาะจงเฉพาะเดือนกันยายน | Bangkok: 540 บาท, Chonburi: 360 บาท |
| **q06.sql** | Dice (ก.ย. + Drink + BKK/Chonburi) | Drink Bangkok: 300 บาท, Drink Chonburi: 200 บาท |
| **q07.sql** | กรองกลุ่มที่ยอดขายเกิน 400 บาท | Bangkok: 540 บาท |
| **q12.sql** | Drill-through แสดงรายละเอียดรายการ | O1005 (Tea 6 ชิ้น = 300 บาท), O1006 (Cookie 3 ชิ้น = 240 บาท) |

### คำตอบคำถามระหว่างทำ
* **ก. q03 vs q04:** q03 เป็นการเพิ่มมิติสถานที่ (ข้ามมิติ) เพื่อเปรียบเทียบเชิงพื้นที่ ส่วน q04 เป็นการเจาะลึกลงในมิติเวลาเดิม (จากเดือนลงไปวัน) ภายใน Hierarchy เดียวกัน ทำให้เห็นพฤติกรรมยอดขายรายวัน
* **ข. WHERE vs HAVING ใน q07:** กรองเดือนใน `WHERE` (คัดกรองระดับแถวเดี่ยวก่อนรวมกลุ่ม) และกรองยอดรวม `SUM(amount) > 400` ใน `HAVING` (คัดกรองผลลัพธ์หลังรวมกลุ่ม)
* **ค. ผลรวม q12:** รายการจาก q12 รวมกัน ($300 + 240 = 540$ บาท) เท่ากับยอดของเดือน **2026-09 จังหวัด Bangkok ใน q03**

---

## 4 Pivot

### ผลการรัน `q08.sql` (SQL Pivot)
```text
province	aug	sep	total
Bangkok	310	540	850
Chonburi	180	360	540
```

### ผลลัพธ์ Python Pivot (`pivot_student.py`)
* **P1 (Province x Month):**
  ```text
  month     2026-08  2026-09  Total
  province                         
  Bangkok       310      540    850
  Chonburi      180      360    540
  Total         490      900   1390
  ```
  บันทึกเป็น `pivot_province_month.csv`
* **P2 (September Category x Province):**
  ```text
  province  Bangkok  Chonburi
  category                   
  Drink         300       200
  Snack         240       160
  ```
  บันทึกเป็น `pivot_september.csv`
* **ผลตรวจ Assert:** `Grand Total (1390) == df['amount'].sum() (1390)` ตรวจสอบผ่านสมบูรณ์

### การทดลองข้อผิดพลาด (aggfunc)
* เมื่อลบ `aggfunc` ออก pandas จะใช้ค่าเริ่มต้นเป็น `'mean'` ทำให้ช่อง Bangkok กันยายนได้ค่า **270** ซึ่งเกิดจาก $(300 + 240) / 2 = 270$
* ค่า 270 แสดงถึง **ยอดขายเฉลี่ยต่อรายการสินค้า** ไม่ใช่ยอดรวม ต้องใส่ `aggfunc='sum'` เพื่อหายอดขายรวมที่ถูกต้อง (540 บาท)

### Excel PivotTable (`pivot.xlsx`)
* ยอดรวมหลังกรอง Drink: **750 บาท**
* การจัดแกน: Rows = `province`, Columns = `month`, Values = `amount (Sum)`
* **ความแตกต่าง sum กับ mean:** `sum` คือผลรวมยอดขายทั้งหมด ส่วน `mean` คือยอดขายเฉลี่ยต่อรายการสินค้า
* **คำถามชวนคิด:** หากเพิ่มเดือนตุลาคม SQL จะ**ไม่แสดงคอลัมน์ใหม่เอง** ต้องแก้ไข SELECT เพิ่ม `CASE WHEN` เอง ส่วน pandas ที่ใช้ `columns='month'` จะตรวจพบค่าใหม่และสร้างคอลัมน์ใหม่อัตโนมัติทันที

---

## 5 ตรวจความถูกต้อง

### ผลการรันคิวรี (q09–q11)
* **q09.sql (UNION ALL):**
  ```text
  month	revenue
  2026-08	490
  2026-09	900
  ALL	1390
  ```
  *คำอธิบาย:* การนำทุกแถวจาก UNION ALL กลับมารวมกันจะเกิด **Double Counting (นับซ้ำสองเท่า)** ได้ยอด $2,780$ บาท เพราะแถว ALL ได้รวมยอดของทุกเดือนไว้แล้ว
* **q10.sql (AOV vs avg_line):**
  ```text
  revenue	orders	aov	avg_line
  1390	6	231.67	173.75
  ```
  *คำอธิบายหน่วย:* AOV คือยอดขายเฉลี่ยต่อคำสั่งซื้อ (**บาทต่อออเดอร์**) ส่วน avg_line คือยอดขายเฉลี่ยต่อรายการสินค้า (**บาทต่อรายการสินค้า/line item**) ค่าต่างกันเพราะ 1 ออเดอร์มีได้หลายรายการสินค้า
* **q11.sql (ตรวจสอบ Fact vs Sales View):**
  ```text
  fact_rows	view_rows	fact_revenue	view_revenue
  8	8	1390	1390
  ```
  ผลตรวจ `PRAGMA foreign_key_check;` คืนค่า `[]` (ไม่พบข้อผิดพลาด)
  *ผลกระทบ:* หากมีคีย์ซ้ำใน Dimension จะเกิด Cartesian Product ทำให้จำนวนแถวและยอดขายเกินจริง (Overstated) หาก FK หาย แถวจะตกหล่นตอน INNER JOIN ทำให้ยอดขายต่ำกว่าจริง (Understated)

### ชนิดของ Measure
1. **Additive:** `amount` รวมได้ทุกมิติ (เวลา สถานที่ สินค้า)
2. **Semi-additive:** สต็อกสินค้าสิ้นวัน (Ending Stock Balance) รวมข้ามสาขาหรือสินค้าได้ แต่ห้ามรวมข้ามเวลา ต้องใช้ค่า ณ วันสิ้นงวด (Snapshot)
3. **Non-additive:** `AOV` หรือเปอร์เซ็นต์กำไร ห้ามนำ AOV แต่ละเดือนมาเฉลี่ยกันตรงๆ ต้องคำนวณใหม่จากยอดรวมทั้งหมดหารด้วยออเดอร์ทั้งหมด

---

## 6 สรุป

### ข้อค้นพบจากข้อมูล
1. **การเติบโตของยอดขาย:** ยอดขายเดือนกันยายน (900 บาท) เติบโตขึ้น 83.67% เมื่อเทียบกับเดือนสิงหาคม (490 บาท) โดยกรุงเทพฯ ครองยอดขายสูงสุดทั้งสองเดือน
2. **สัดส่วนยอดขายสินค้า:** ยอดขายหมวด Drink (500 บาท) และ Snack (400 บาท) ในเดือนกันยายนมีสัดส่วนสมดุลกัน

### ข้อจำกัดของข้อมูล
* ข้อมูลจำลองมีขนาดเล็ก (8 รายการ 2 เดือน) และไม่มีข้อมูลต้นทุนสินค้าหรือรหัสลูกค้า ทำให้ยังไม่สามารถวิเคราะห์พฤติกรรมการซื้อซ้ำ (Retention) หรืออัตรากำไรสุทธิ (Profit Margin) ได้

### การใช้ AI
* ใช้ AI ช่วยตรวจสอบ syntax ในการแปลงทศนิยมของ SQLite (`CAST(... AS REAL)`) ใน `q10.sql` และช่วยตรวจเช็คโครงสร้าง Star Schema โดยผู้ทำแลปได้ทบทวน ทำความเข้าใจ และรัน assert ทดสอบความถูกต้องด้วยตนเองทุกข้อ

---

## 7 โจทย์ต่อยอดนอกเวลา (Challenge Task)

ไฟล์สคริปต์: `challenge.py`
* **ก. วิเคราะห์ `extended.db`:**
  * 3 สาขายอดขายสูงสุด: **1. Nimman (145,280 บาท)**, **2. Pattaya (141,215 บาท)**, **3. Muang Rayong (136,710 บาท)**
  * เปรียบเทียบ AOV: ค่าเฉลี่ยของ AOV รายเดือนได้ **449.05 บาท** ในขณะที่ AOV รวมจริงของทั้งช่วงคือ **448.86 บาท** ความแตกต่างนี้เกิดขึ้นเพราะแต่ละเดือนมีจำนวนออเดอร์ไม่เท่ากัน (การเฉลี่ยของค่าเฉลี่ยไม่ได้ถ่วงน้ำหนักตามจำนวนออเดอร์)
* **ข. สำเนา `challenge.db` และเพิ่มข้อมูลเดือนตุลาคม:**
  * เพิ่มข้อมูลวันที่ `2026-10-01` และออเดอร์ `O1007`, `O1008` รวม 3 รายการ ผ่านการตรวจ Foreign Key สำเร็จ
  * ข้อมูลรวมกลายเป็น **11 รายการ, 8 ออเดอร์, ยอดขายรวม 1,900 บาท**
  * สร้างและส่งออก `pivot_challenge.csv` สำเร็จสมบูรณ์
